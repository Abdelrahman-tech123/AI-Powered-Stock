this is simply an template for web pages of full stack

in forntend 
    I used next.js with typescript and applying to it basic login and signup pages with safe dashboard page that can be enterd bu Authenicated users only
in backend 
    I used fastAPI , I also make the basic of simple auth system with emails and hashed passwords and strong UUID passwords and linking the user to the db directly by just add the db link to the .env

Note :
    Run the server with run.py wich solves many problems like problems related to playwright , windows and auto refresh for changes